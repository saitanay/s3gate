[package]
name = "s3gate-proxy"
version = "0.1.0"
edition = "2021"

[dependencies]
pingora = { version = "0.8", features = ["proxy"] }
async-trait = "0.1"
log = "0.4"
env_logger = "0.11"

[profile.release]
opt-level = 3
lto = true
strip = true
