services:
  bucketcheap:
    build: .
    container_name: bucketcheap
    ports:
      - "9000:9000"
    volumes:
      - upload-cache:/data/uploads
      - sqlite-data:/data/db
    environment:
      - STORAGEBOX_HOST=u613760-sub1.your-storagebox.de
      - STORAGEBOX_USERNAME=u613760-sub1
      - STORAGEBOX_PASSWORD=Tanay#5566778899
      - SMTP_HOST=smtp.purelymail.com
      - SMTP_PORT=587
      - SMTP_USER=no-reply@postcard.rest
      - SMTP_PASSWORD=jWUEQ8YaHSvifpg
      - SMTP_FROM=no-reply@postcard.rest
      - CASHFREE_APP_ID=${CASHFREE_APP_ID}
      - CASHFREE_SECRET_KEY=${CASHFREE_SECRET_KEY}
      - BASE_URL=https://bucketcheap.com
      - ADMIN_EMAIL=bucketcheap@tanay.co.in
      - ADMIN_PASSWORD=BucketCheap2026
      - PORT=9000
      - DB_PATH=/data/db/bucketcheap.db
      - TEMPLATE_DIR=/app/web/templates
    restart: unless-stopped

volumes:
  upload-cache:
  sqlite-data:
